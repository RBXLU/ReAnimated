package com.pycodder.reanimated.anim;

/** Access to a container screen panel's vertical bounds (top/bottom in screen coordinates). */
public interface PanelBounds {
    int reanimated$panelTop();
    int reanimated$panelBottom();
}
